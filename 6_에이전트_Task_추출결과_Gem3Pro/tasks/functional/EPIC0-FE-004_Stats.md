# EPIC0-FE-004: 통계 대시보드 및 스트릭 UI

## 1. 개요
사용자의 달성 기록을 시각화하는 통계 화면과, 연속 달성일(스트릭)을 보여주는 UI 요소를 구현합니다.

## 2. 주요 기능 범위
- **스트릭 배지**: 메인 화면 또는 상단 바에 '🔥 5일 연속' 형태의 배지 표시.
- **통계 탭**: 하단 탭 바의 '통계' 섹션.
- **주간/월간 그래프**: 막대 그래프(Bar Chart)로 일별 달성률 표시.
- **요약 카드**: 총 집중 시간, 성공 횟수 등 요약 정보 표시.

## 3. 구현 가이드 (Steps Hint)
1.  **StreakView**: 불꽃 아이콘과 숫자를 포함한 작은 캡슐 형태 뷰.
2.  **Charts**: Swift Charts 프레임워크 또는 `HStack`/`VStack`과 `Rectangle`을 이용한 커스텀 막대 그래프 구현.
    - Mock 데이터: 지난 7일간의 완료 스케줄 수.
3.  **StatsDashboard**: 스크롤 가능한 뷰에 그래프와 요약 카드를 배치.
4.  **Interaction**:
    - 통계 탭 터치 시 화면 전환.
    - 그래프 터치 시 상세 수치 표시(Tooltip) - 선택 사항.

## 4. 제약사항 및 제외 범위
- **제외**: 서버 통계 API 연동.
- **제외**: 복잡한 날짜 필터링 로직 (이번 주 데이터만 고정 표시).

---

```yaml
task_id: "EPIC0-FE-004"
title: "통계 대시보드 및 스트릭 UI 프로토타입 구현"
summary: >
  사용자의 성취감을 고취시키기 위한 스트릭 배지와 주간 달성률 그래프 UI를 구현한다.
type: "functional"
epic: "EPIC_0_FE_PROTOTYPE"
req_ids: ["REQ-FUNC-019", "REQ-FUNC-020"]
component: ["frontend.ui", "frontend.charts"]

context:
  srs_section: "4.1 기능 요구사항 Story 6"
  user_story: "사용자는 자신의 성과를 시각적으로 확인하고 동기를 부여받을 수 있어야 한다."

inputs:
  description: "통계 조회 요청"
  mock_data:
    - "Daily Stats: [{date: 'Mon', count: 3}, {date: 'Tue', count: 5}, ...]"
    - "Current Streak: 7"

outputs:
  description: "Stats View"
  success_criteria:
    - "스트릭 숫자가 메인 화면에 표시되어야 한다."
    - "통계 탭에서 막대 그래프가 정상 렌더링되어야 한다."

preconditions:
  - "Swift Charts 라이브러리(iOS 16+) 사용 가능 확인."

postconditions:
  - "통계 화면 레이아웃이 깨짐 없이 표시된다."

config:
  feature_flags: []

tests:
  manual:
    - "통계 탭 진입 확인."
    - "Mock 데이터를 변경했을 때 그래프 높이가 변하는지 확인."

dependencies: []
parallelizable: true
estimated_effort: "S"
priority: "Should"
agent_profile: ["frontend"]
required_tools:
  languages: ["Swift"]
  frameworks: ["SwiftUI", "Swift Charts"]
```

